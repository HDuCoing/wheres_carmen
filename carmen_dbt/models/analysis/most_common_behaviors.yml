version: 2

models:
  - name: most_common_behaviors
    description: ""
    columns:
      - name: sightings_count
        description: "{{ doc('sightings_count') }}"
        data_type: integer
      - name: behavior
        description: "{{ doc('behavior') }}"
        data_type: string
      - name: date_witness
        description: "{{ doc('date_witness') }}"
        data_type: date
